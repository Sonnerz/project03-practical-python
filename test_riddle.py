import unittest

class TestRiddle(unittest.TestCase):
    def test_is_this_thing_on(self):
        self.assertEqual(1,1)
        
    def test_is_in(self):
        assert item in collection, "{0} does not contain {1}".format(collection, item)
        

print(test_is_in(["dog", "cat", "bird"], "fish"))        
        
        